import styled from "styled-components";
import { BaseButton } from "../../../../Styled";
function Button() {
  return <Btn>Conheça nossos Parceiros</Btn>;
}

const Btn = styled(BaseButton)`
`;

export default Button;
